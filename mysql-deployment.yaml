apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql-container
          image: mysql:5.7
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                 name: laravel-db-secrets
                 key: DB_PASSWORD
            - name: MYSQL_DATABASE
              valueFrom:
                secretKeyRef:
                 name: laravel-db-secrets
                 key: DB_DATABASE
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                 name: laravel-db-secrets
                 key: DB_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                 name: laravel-db-secrets
                 key: DB_PASSWORD
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
spec:
  selector:
    app: mysql
  ports:
   - protocol: TCP
     port: 3306
  clusterIP: None
 